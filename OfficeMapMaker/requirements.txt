# Runtime dependencies
opencv-python>=4.8
pillow>=10.0
numpy>=1.24
pytesseract>=0.3.10
openpyxl>=3.1
xlrd==1.2.0          # only version that still reads legacy BIFF .xls
reportlab>=4.0
PySide6>=6.6           # interactive calibration editor (Qt6 binding, LGPL)

# Dev / test only
pytest>=7.4
pytest-qt>=4.4         # boots a QApplication for editor smoke tests
xlwt>=1.3              # only used in tests to generate synthetic .xls fixtures
